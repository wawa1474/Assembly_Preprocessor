A 'simple' preprocessor written in Processing Java (no affiliation), meant to transform a more readable source file into something that can be assembled using an assembler of your choice.

The goal was to be able to write high(er)-level code in an easy to read, debug, and generally understand format, that can then be processed into a form that an external assembler can understand.